# Copy this file to one of:
# - .env.development (used by `pnpm dev`)
# - .env.production  (used by `pnpm run build` / CI)
# - .env             (base defaults; can be overridden by mode-specific files)
#
# All variables must be prefixed with VITE_ to be exposed to the client.

# Proxy path for dev or edge deployments (leave empty for production/CDN)
# Example: /proxy or ""
VITE_PROXY_TAG=

# App base URL (used by router/assets in prod builds)
# Example: https://app.innergy.com or /
VITE_BASE_URL=

# API key (optional). Prefer secrets management in CI/hosting.
VITE_NRG_API_KEY=

# Feature flags
# Show/hide the experimental Ticket Planner page.
# - true  => show the Plan button and route
# - false => hide in UI entirely
VITE_ENABLE_TICKET_PLANNER=false
